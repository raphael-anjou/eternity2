# Native debug binary built by `./build.sh --native` for golden diffing.
# Not needed by the site (engine.wasm is the shipped artifact) and platform-
# specific, so it is not committed. Reproduce it any time with build.sh.
engine_native
