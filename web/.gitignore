# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Anchor to the repo root: the React Router build output is /build. A bare
# "build" also ignored the research/build/ SOURCE pages (the "Build a solver"
# door), silently dropping them from git. Keep this anchored.
/build
.react-router
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results/
playwright-report/
