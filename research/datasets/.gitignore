# The strong-board corpus is distributed as the committed zip
# (e2-strong-boards.zip). These loose files are build artifacts of
# build/build_dataset.py, fully contained in the zip, so they are not tracked.
corpus/boards.csv
corpus/boards.jsonl
