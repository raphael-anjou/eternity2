# Reproduction outputs (the committed run lives in results/ directly).
results/rerun/
results/smoke/
results/*_grid.log
# Working-state / scratch notes (not part of the published record).
results/NOTES.md
results/scratch*
# Rust build artifacts (also covered by the root target/ ignore).
engine/target/
# Python bytecode cache
__pycache__/
*.pyc
